name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
      # Keep compiling and running the black-box suite when formatting fails so a
      # formatting-only regression does not hide more consequential failures.
      - id: fmt
        run: cargo fmt --all -- --check
        continue-on-error: true
      # Format the ephemeral checkout so compiler/test diagnostics are not
      # hidden by a formatting-only failure. The final gate still fails CI.
      - run: cargo fmt --all
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - if: always()
        run: cargo test --all-targets --all-features -- --test-threads=1
      - name: Preserve rustfmt output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: formatted-source-${{ github.sha }}
          path: |
            src
            tests
          if-no-files-found: error
      - name: Preserve resolved dependency lockfile
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cargo-lock-${{ github.sha }}
          path: Cargo.lock
          if-no-files-found: error
      - name: Require Rust formatting
        if: always()
        run: test "${{ steps.fmt.outcome }}" = success
